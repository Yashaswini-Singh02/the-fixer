{
  "name": "the-fix",
  "private": true,
  "packageManager": "pnpm@11.10.0",
  "type": "module",
  "scripts": {
    "test": "pnpm -r test",
    "record": "pnpm --filter @thefix/server record",
    "serve": "pnpm --filter @thefix/server serve"
  },
  "devDependencies": {
    "typescript": "^5.6.0"
  }
}
