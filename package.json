{
  "name": "the-fix",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "pnpm -r test",
    "record": "pnpm --filter @thefix/server record",
    "serve": "pnpm --filter @thefix/server serve"
  },
  "devDependencies": {
    "typescript": "^5.6.0"
  }
}
